# Changesets

We use [Changesets](https://github.com/changesets/changesets) for versioning and publishing.

- First release (`0.1.0`): see [NPM_PUBLISH.md](../NPM_PUBLISH.md) — publish current versions as-is
- Later releases: `pnpm changeset` → `pnpm changeset version` → `pnpm release`
