# CLI project templates

Templates used by `create-nexorajs` will live here.

Current scaffold logic is embedded in `packages/cli`.
Externalized templates (default, with-dashboard, minimal) are planned.
