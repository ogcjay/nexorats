# When Git Sync Project directory = repository root (.)
# Prefer setting Project directory to "apps/docs" instead (see apps/docs/.gitbook.yaml).

root: ./apps/docs/

structure:
  readme: README.md
  summary: SUMMARY.md
